#include <stdio.h>

int main()
{
    int i, j;
    for (i=0; i<100, j!=3; i++, j++){
        printf("i=%d\n", i);
    }

    return 0;
}
迴圈會在 i 小於 100 且 j 不等於 3 的情況下繼續執行。每次迴圈執行完後，i 會加 1，j 會加 1。
當迴圈執行第三次時j2等於三，於是停止執行
